<div class="container">
  <h3><%= @customer.last_name %><%= @customer.first_name %>さんの会員詳細</h3>
  <div class="row">
    <div>
      <div class="col-xs-7">
        <p>会員ID <%= @customer.id %></p>
        <p>氏名 <%= @customer.last_name %> <%= @customer.first_name %></p>
        <p>フリガナ <%= @customer.last_name_kana %> <%= @customer.first_name_kana %></p>
        <p>郵便番号 <%= @customer.postal_code %></p>
        <p>住所 <%= @customer.address %></p>
        <p>電話番号 <%= @customer.telephone_number %></p>
        <p>メールアドレス <%= @customer.email %></p>
        <p>
          会員ステータス
          <% if @customer.is_deleted == false %>
            有効
          <% else %>
            退会
          <% end %>
        </p>
      </div>
    </div>
  </div>
  <span><%= link_to "編集する", edit_admin_customer_path(@customer.id), class: "btn btn-success" %></span>
  <span><%= link_to "注文履歴一覧を見る", admin_root_path, class: "btn btn-primary" %></span>
</div>
